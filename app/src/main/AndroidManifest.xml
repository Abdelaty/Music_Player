<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abdel.musicplayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".ArtistsActivity"
            android:label="@string/arabic"

            />
        <activity
            android:name=".EnglishArtistsActivity"
            android:label="@string/english" />
        <activity
            android:name=".OmKalthoumActivity"
            android:label="Om Khartoum" />
        <activity
            android:name=".EminemActivity"
            android:label="@string/eminem" />
        <activity
            android:name=".RevivleEminem"
            android:label="@string/Revivle" />

    </application>

</manifest>